// this is a client side rendered component
'use client';
import React from 'react'

const AddToWatchlist = () => {
  return (
    <div>
      <button onClick={() => console.log('Click')}>Add to Watchlist</button>
    </div>
  )
}

export default AddToWatchlist